apiVersion: apps/v1
kind: Deployment
metadata:
   name: frontend
spec:
   template:
     metadata:
       labels:
         run: frontend
     spec:
       containers:
       - name: frontend
         image: aymen2310/employee
         ports:
         - containerPort: 8888
    
